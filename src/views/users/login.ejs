<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
        <link rel="stylesheet" href="/css/styles-LoginRegister.css">
        <title>Ingresá a tu cuenta</title>
</head>

<body>
    <header>
        <div class="header__container">
            <div class="header__logo">
                <a href="/">
                    <img src="/images/logo-golosinas-mailin.svg" class="header__logo--img" alt="golosinas-mailin" />
                </a>
            </div>

        </div>

    </header>
    <main>
        <div class="main__enlaces">
            <a href="/">Inicio </a><span class="span"> > </span><a href="/user/login"> Login</a>
        </div>

        <div class="main-form">
        
            <form action="/user/login" method="post" class="formulario">
                <h2 class="title-form">Ingresá a tu cuenta</h2>

                <% if (locals.message) { %>
                    <div class="mensaje">
                        <i class="fa-solid fa-check"></i> <%=  message %>
                    </div>
                <% } %>

                <% if (locals.errors && errors.general) { %>
                    <small class="text-danger error"><%= errors.general.msg %></small>
                <% } %>

                <div class="main__form-group ">
                    <label for="email">Email</label>
                    <div class="input-with-icon">
                        <input type="text" name="email" value="<%= locals.oldData ? oldData.email : '' %>"
                         class=" <%= locals.errors && locals.errors.email? 'is-invalid' : null %>">
                         <% if (locals.errors && locals.errors.email) { %>
                            <i class="fa-solid fa-circle-exclamation"></i>
                        <% } %>
                    </div>
                    <% if (locals.errors && errors.email) { %>
                        <small class="text-danger"><%= errors.email.msg  %></small> 
                    <% } %>
                </div>

                <div class="main__form-group">
                    <label for="password">Contraseña</label>
                    <div class="input-with-icon">
                        <input type="password" name="password" class="password <%= locals.errors && locals.errors.password? 'is-invalid' : null %>">
                        <% if (locals.errors && locals.errors.password) { %>
                            <i class="fa-solid fa-circle-exclamation"></i>
                        <% } %>
                    </div>
                    <% if (locals.errors && errors.password) { %>
                        <small class="text-danger">
                            <%= errors.password.msg  %>
                        </small> 
                    <% } %>
                </div>
                <div class="main__recordarUsuario">
                    <label for="recordarUsuario"><input type="checkbox" id="recordarUsuario" name="recordarUsuario"> Recordar mi usuario</label>
                    <a href="/user/login?action=resetpassword" class="lostPassword">¿Olvidaste la contraseña?</a>
                </div>
                <div class="main__form-group">
                    <button type="submit" class="button-submit">Ingresar</button>
                </div>
            </form>

            <p class="main__options">¿Aún no tienes cuenta? <a href="/user/register">Regístrate</a></p>
        </div>
    </main>

    <%- include('../partials/footer') %>

</body>

</html>